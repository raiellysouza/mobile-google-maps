package com.example.myplacesapp

internal class {
    internal inner class MyPlacesViewModel

    fun ViewModel() {
        val MyPlaces: Boolean = mutableStateListOf<MyPlace>()


        val selecPlaces: Boolean = mutableStateListOf<MyPlace>()


        val toggleSelection: `fun`?
        (place)
        MyPlace
        run {
            if (selectedPlaces.contains(place)) {
                selectedPlaces.remove(place)
            } else {
                if (selectedPlaces.size < 2) {
                    slectedPlaces.add(place)
                } else {
                    slectPlaces.clear()
                    selectPlaces.add(place)
                }
            }
        }
    }
}